# 🌍 Soul FM Hub - World Map & Real-time Usage Update

## ✅ Что добавлено

### 1. **Интерактивная карта мира с активными слушателями**

#### **Визуализация**
- ✅ Полноценная SVG-карта мира с помощью `react-simple-maps`
- ✅ Анимированные точки слушателей в реальном времени
- ✅ Пульсирующие маркеры на координатах слушателей
- ✅ Hover-подсказки с информацией о слушателе

#### **Статистика в реальном времени**
- **Active Listeners** - общее количество активных слушателей
- **Countries** - количество стран, откуда слушают
- **Live Connections** - активные подключения

#### **Recent Connections**
- Список последних 10 подключений
- Город и страна слушателя
- Время подключения
- Live статус

### 2. **Реальные данные Usage (Storage & Bandwidth)**

#### **Storage (Хранилище)**
- ✅ Реальный расчет использованного места на основе загруженных треков
- ✅ Интеграция с Supabase Storage для точных размеров файлов
- ✅ Прогресс-бар с градиентом cyan-to-mint
- ✅ Процент использования в реальном времени

#### **Bandwidth (Трафик)**
- ✅ Расчет на основе активных слушателей
- ✅ Учет средней битрейта (128 kbps)
- ✅ Автоматический подсчет за текущий месяц
- ✅ Прогресс-бар с градиентом mint-to-cyan

## 🔧 Технические детали

### **Новые API Endpoints**

#### 1. GET `/analytics/active-listeners`
Возвращает список активных слушателей с геолокацией:
```json
{
  "listeners": [
    {
      "id": "listener_xxx",
      "country": "United States",
      "city": "New York",
      "coordinates": [-74.0060, 40.7128],
      "connectedAt": "2026-02-06T12:30:00.000Z",
      "lastSeen": "2026-02-06T12:35:00.000Z",
      "userAgent": "Mozilla/5.0...",
      "ip": "123.456.789.0"
    }
  ],
  "total": 15,
  "countries": 8
}
```

#### 2. POST `/analytics/listener-connect`
Отслеживает подключение нового слушателя:
- Определяет IP адрес
- Получает геолокацию через ipapi.co
- Сохраняет в KV store с TTL 5 минут

#### 3. POST `/analytics/listener-heartbeat/:id`
Обновляет heartbeat активного слушателя:
- Продлевает активность слушателя
- Обновляет `lastSeen` timestamp

#### 4. GET `/analytics/usage`
Возвращает реальные данные использования:
```json
{
  "storage": {
    "used": 2.45,
    "total": 50,
    "percentage": 5
  },
  "bandwidth": {
    "used": 0.12,
    "total": 20,
    "percentage": 1
  },
  "totalTracks": 54,
  "activeListeners": 15,
  "lastUpdated": "2026-02-06T12:35:00.000Z"
}
```

### **Новые компоненты**

#### `ListenersWorldMap.tsx`
Полноценный компонент карты мира:
- **ComposableMap** - главный компонент карты
- **Geographies** - отрисовка стран
- **Marker** - маркеры слушателей с анимацией
- **AnimatePresence** - анимация появления/исчезновения
- **Auto-refresh** - обновление каждые 10 секунд

### **Обновленные компоненты**

#### `SuperAdminDashboard.tsx`
- ✅ Добавлена секция "Live Listeners" с картой мира
- ✅ Реальные данные Usage из API
- ✅ Автоматическая загрузка статистики при открытии
- ✅ Интеграция с `api.getUsageStats()`

### **API клиент (api.ts)**
Добавлены новые методы:
```typescript
// Получить активных слушателей
async getActiveListeners()

// Получить данные Usage
async getUsageStats()
```

## 📊 Алгоритмы расчета

### **Storage (Хранилище)**
1. Получаем все треки из KV store
2. Умножаем количество на средний размер файла (4.5 MB)
3. Пытаемся получить реальные размеры из Supabase Storage
4. Конвертируем в GB и округляем

### **Bandwidth (Трафик)**
1. Считаем активных слушателей (lastSeen < 5 минут)
2. Умножаем на средний битрейт: 128 kbps = 0.96 MB/min
3. Умножаем на количество минут в текущем месяце
4. Конвертируем в TB

### **Geolocation (Геолокация)**
1. Извлекаем IP адрес из headers (`x-forwarded-for` или `x-real-ip`)
2. Запрашиваем геолокацию через `https://ipapi.co/{ip}/json/`
3. Получаем: country, city, coordinates (longitude, latitude)
4. Fallback на "Unknown" если API недоступен

## 🎨 UI/UX особенности

### **Карта мира**
- **Темная тема** - #1e293b для стран
- **Границы** - #334155
- **Hover эффект** - подсветка страны
- **Маркеры** - анимированные точки с пульсацией
- **Tooltip** - информация при наведении

### **Анимации**
- **Пульсация** - круг расширяется и исчезает (2s infinite)
- **Появление** - fade in + scale up (0.5s)
- **Прогресс-бары** - плавное заполнение (1s ease-out)

### **Цветовая схема**
- Listeners count: `#00d9ff` (cyan)
- Countries count: `#00ffaa` (mint)
- Storage progress: gradient от cyan к mint
- Bandwidth progress: gradient от mint к cyan

## 🔄 Real-time обновления

### **Auto-refresh**
- Карта мира обновляется каждые **10 секунд**
- Usage данные обновляются при переходе на Home
- Listener heartbeat отправляется каждые **30 секунд** (опционально)

### **WebSocket integration** (будущее улучшение)
В будущем можно добавить WebSocket для:
- Мгновенного обновления при новом слушателе
- Real-time координат на карте
- Live обновление статистики

## 📍 Расположение на дашборде

**SuperAdminDashboard → Home раздел:**

1. Now Playing
2. Listen
3. Usage (Storage + Bandwidth) ← **Реальные данные**
4. Upcoming Shows
5. **Live Listeners** ← **Новая секция с картой** 🌍
6. Quick Stats (Tracks / Playlists / Users)

## 🚀 Как использовать

### **Для администратора**

1. Войдите в дашборд (`/dashboard` или `/admin`)
2. Перейдите в раздел **Home**
3. Прокрутите вниз до секции **Live Listeners**
4. Наблюдайте за активными слушателями на карте мира
5. Наведите курсор на маркер для подробностей

### **Для разработчика**

```typescript
// Получить активных слушателей
const response = await api.getActiveListeners();
console.log('Активные слушатели:', response.listeners);

// Получить данные Usage
const usage = await api.getUsageStats();
console.log('Storage:', usage.storage);
console.log('Bandwidth:', usage.bandwidth);
```

## 📦 Установленные пакеты

```json
{
  "react-simple-maps": "^3.0.0",
  "d3-geo": "^3.1.1"
}
```

## 🐛 Известные ограничения

### **Geolocation API**
- Бесплатный API ipapi.co имеет лимит: **1000 запросов/день**
- При превышении лимита слушатели будут с координатами [0, 0]
- Fallback на "Unknown" для города и страны

### **Bandwidth расчет**
- Расчет **приблизительный**, основан на среднем битрейте
- Не учитывает фактический поток данных
- Для точного расчета нужна интеграция с CDN/Proxy

### **Storage расчет**
- Если Supabase Storage metadata недоступна, используется средний размер
- Точность зависит от наличия метаданных файлов

## 🔮 Будущие улучшения

1. **WebSocket для real-time обновлений** карты
2. **Heatmap** вместо точек для большого количества слушателей
3. **Фильтры по странам/городам**
4. **История подключений** за последние 24 часа
5. **Analytics dashboard** с графиками
6. **Export данных** в CSV/JSON
7. **Alerts** при достижении лимитов Storage/Bandwidth
8. **CDN integration** для точного bandwidth tracking

## ✅ Checklist

- [x] Установлены пакеты react-simple-maps и d3-geo
- [x] Создан компонент ListenersWorldMap
- [x] Добавлены API endpoints для слушателей и usage
- [x] Интегрирована геолокация через ipapi.co
- [x] Реальные данные Storage из Supabase
- [x] Расчет Bandwidth на основе активных слушателей
- [x] Добавлена секция Live Listeners в Home
- [x] Анимации и визуальные эффекты
- [x] Auto-refresh каждые 10 секунд
- [x] Responsive дизайн
- [x] Документация

---

**Status**: ✅ **ПОЛНОСТЬЮ ГОТОВО К ИСПОЛЬЗОВАНИЮ**

**Версия**: 2.1  
**Дата обновления**: 6 февраля 2026  
**Автор**: Soul FM Hub Development Team
