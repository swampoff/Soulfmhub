# 🚀 Soul FM Hub - Быстрая настройка Super Admin

## Шаг 1: Регистрация пользователя

1. Откройте приложение и перейдите на страницу `/auth`
2. Зарегистрируйте пользователя:
   - **Email**: `niqbello@gmail.com`
   - **Password**: `NIk4873835`
   - **Name**: Niko Bello

## Шаг 2: Назначение роли Super Admin

### Вариант А: Через Admin Dashboard (после входа)

1. Войдите в систему с любым аккаунтом
2. Перейдите на `/admin` (или `/dashboard`)
3. В верхней части страницы найдите карточку "🔐 Assign Super Admin Role"
4. Введите email: `niqbello@gmail.com`
5. Нажмите кнопку "Assign Admin"
6. Дождитесь подтверждения: "✅ Super Admin assigned"

### Вариант Б: Через Browser Console

1. Откройте DevTools (F12)
2. Перейдите в Console
3. Выполните следующий код:

```javascript
// Функция для назначения super admin
async function setupAdmin() {
  const response = await fetch(
    `${window.location.origin.replace('localhost:5173', window.location.hostname.includes('localhost') ? 'localhost:54321' : window.location.hostname)}/functions/v1/make-server-06086aa3/admin/assign-super-admin`,
    {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({ email: 'niqbello@gmail.com' }),
    }
  );
  
  const data = await response.json();
  console.log('✅ Result:', data);
  return data;
}

// Выполните
setupAdmin();
```

### Вариант В: Прямой API вызов (curl)

```bash
# Замените YOUR_PROJECT_ID и YOUR_ANON_KEY на реальные значения
curl -X POST "https://YOUR_PROJECT_ID.supabase.co/functions/v1/make-server-06086aa3/admin/assign-super-admin" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_ANON_KEY" \
  -d '{"email":"niqbello@gmail.com"}'
```

## Шаг 3: Вход как Super Admin

1. Выйдите из текущего аккаунта (если вы вошли)
2. Перейдите на `/auth`
3. Войдите с учетными данными:
   - **Email**: `niqbello@gmail.com`
   - **Password**: `NIk4873835`
4. Вы будете перенаправлены на Dashboard с полным доступом администратора

## Шаг 4: Проверка доступа

После входа вы должны иметь доступ к:

- ✅ `/admin` - Админ дашборд
- ✅ `/admin/tracks` - Управление треками (с функцией массовой загрузки)
- ✅ `/admin/playlists` - Управление плейлистами
- ✅ `/admin/schedule` - Расписание эфира
- ✅ `/admin/shows` - Управление шоу
- ✅ `/admin/news` - Управление новостями
- ✅ `/admin/donations` - Донаты

## Функции Super Admin

### 1. Массовая загрузка треков

- Перейдите на `/admin/tracks`
- Используйте Drag & Drop для загрузки MP3 файлов
- Автоматическое извлечение метаданных из имени файла (формат: "Artist - Title.mp3")
- Автоматическое присвоение тега "NEWFUNK"
- Генерация короткой ссылки типа `soulfm.stream/a3f7x2`
- Опция "Auto-add to Live Stream" для добавления в основной плейлист

### 2. Управление плейлистами

- Создание/редактирование плейлистов по жанрам
- Специальный плейлист "Live Stream" для основного эфира
- Drag & drop сортировка треков

### 3. Управление пользователями

- Просмотр всех зарегистрированных пользователей
- Назначение ролей: DJ, Host, Music Curator, Content Manager, Program Director
- Удаление пользователей

### 4. Аналитика

- Статистика прослушиваний
- Топ треки
- Активность пользователей
- Статистика донатов

## Технические детали

### Endpoints для работы с ролями

**Назначение Super Admin:**
```
POST /make-server-06086aa3/admin/assign-super-admin
Body: { "email": "user@example.com", "secretKey": "optional" }
```

**Проверка пользователя:**
```
GET /make-server-06086aa3/admin/user-by-email?email=user@example.com
```

**Обновление роли:**
```
PUT /make-server-06086aa3/users/:userId/role
Body: { "role": "dj" }
```

### Система ролей

```typescript
type UserRole = 
  | 'listener'        // Обычный слушатель (по умолчанию)
  | 'dj'             // DJ - загрузка треков, плейлисты
  | 'host'           // Ведущий - управление шоу
  | 'music_curator'  // Музыкальный куратор
  | 'content_manager'// Менеджер контента
  | 'program_director' // Программный директор
  | 'super_admin';   // Полный доступ
```

## Troubleshooting

### ❌ "User not found"
**Решение:** Сначала зарегистрируйте пользователя через `/auth`, затем назначьте роль

### ❌ "No access to admin panel"
**Решение:** 
1. Проверьте что роль назначена успешно
2. Полностью выйдите из системы
3. Очистите cookies и localStorage
4. Войдите снова

### ❌ "Failed to assign super admin"
**Решение:** Проверьте что:
- Backend сервер запущен
- Email написан правильно
- Пользователь уже зарегистрирован

### ❌ Треки не загружаются
**Решение:**
- Проверьте формат файлов (MP3, WAV, M4A, FLAC)
- Убедитесь что вы вошли как пользователь с правами DJ или выше
- Проверьте размер файла (рекомендуется до 50MB)

## Что дальше?

После успешной настройки вы можете:

1. **Загрузить треки:**
   - Перейдите на `/admin/tracks`
   - Drag & drop MP3 файлы
   - Включите "Auto-add to Live Stream"

2. **Создать плейлисты:**
   - Funk, Soul, Jazz, etc.
   - Настроить Live Stream playlist

3. **Настроить расписание:**
   - Создать Live Shows
   - Добавить Podcasts
   - Настроить Auto DJ

4. **Интегрировать Icecast:**
   - См. `/ICECAST_INTEGRATION.md`
   - Настроить streaming server
   - Подключить Auto DJ

5. **Кастомизировать дизайн:**
   - Обновить логотип
   - Настроить цветовую схему
   - Добавить свои анимации

## Контакты

Email: niqbello@gmail.com  
Password: NIk4873835  
Role: super_admin

**Готово! 🎉 Теперь у вас есть полный доступ к Soul FM Hub!**
