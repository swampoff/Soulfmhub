# 🔧 Environment Variables Setup Guide

## ✅ Шаг 1: VITE_STREAM_URL уже настроен!

**Секрет VITE_STREAM_URL был создан через Figma Make!** ✅

Теперь нужно **ввести значение** для этой переменной.

---

## 🎵 Варианты Stream URL

### 🔥 Вариант 1: Test Stream (для немедленного тестирования)

Используй один из публичных test stream'ов:

```bash
# Soul/Funk радио (рекомендуется для тестирования Soul FM)
https://icecast.streamserver24.com:8000/soul128.mp3

# Smooth Jazz (альтернатива)
https://stream.srg-ssr.ch/m/rsj/mp3_128

# Chill/Lounge
http://stream.zeno.fm/f3wvbbqmdg8uv
```

### 🚀 Вариант 2: Свой Icecast Server (для production)

Если у тебя есть свой Icecast сервер:

```bash
# Формат:
http://your-domain.com:port/mount

# Пример:
http://stream.soulfm.radio:8000/stream

# Или с SSL (рекомендуется):
https://stream.soulfm.radio/stream
```

### 🌐 Вариант 3: Облачные сервисы

Популярные облачные радио платформы:

1. **Azuracast** (бесплатный, self-hosted)
   - URL: `https://your-station.azuracast.com/radio/8000/stream`

2. **Radio.co** (платный, managed)
   - URL: `https://stream.radio.co/your-station/listen`

3. **Shoutcast** (классика)
   - URL: `http://your-ip:8000/;`

---

## 📝 Как ввести значение:

### Через Figma Make UI:

1. Открой **Settings** → **Environment Variables**
2. Найди `VITE_STREAM_URL`
3. Введи одно из значений выше
4. Сохрани

### Или локально (для разработки):

Создай файл `.env` в корне проекта:

```bash
# .env
VITE_STREAM_URL=https://icecast.streamserver24.com:8000/soul128.mp3
```

---

## 🧪 Как протестировать:

1. Установи значение `VITE_STREAM_URL`
2. Перезапусти приложение
3. Открой главную страницу
4. Нажми **Play** на радиоплеере внизу
5. Должен заиграть stream! 🎵

---

## ⚠️ Важные моменты:

### CORS Headers

Если используешь свой Icecast, настрой CORS в `icecast.xml`:

```xml
<http-headers>
    <header name="Access-Control-Allow-Origin" value="*" />
    <header name="Access-Control-Allow-Headers" value="Origin, Accept, X-Requested-With, Content-Type" />
    <header name="Access-Control-Allow-Methods" value="GET, OPTIONS, HEAD" />
</http-headers>
```

### SSL Certificate

Для production **обязательно** используй HTTPS:
- Пользователи не смогут слушать HTTP stream на HTTPS сайте
- Браузеры блокируют mixed content
- Используй Let's Encrypt для бесплатного SSL

### Bitrate

Рекомендуемые настройки:
- **128 kbps MP3** - оптимально для большинства
- **320 kbps MP3** - максимальное качество (больше трафика)
- **64 kbps MP3** - для экономии трафика

---

## 🎯 Рекомендация для быстрого старта:

**Используй test stream для начала:**

```
VITE_STREAM_URL=https://icecast.streamserver24.com:8000/soul128.mp3
```

Это позволит протестировать весь функционал без настройки своего сервера!

Потом, когда всё заработает, замени на свой реальный Icecast URL.

---

## 🆘 Troubleshooting:

### Проблема: "Stream not loading"
**Решение:**
- Проверь CORS headers на Icecast
- Убедись что URL доступен (открой в браузере)
- Проверь firewall/port forwarding

### Проблема: "Error playing audio"
**Решение:**
- Убедись что формат MP3 (не OGG/AAC)
- Проверь bitrate (не больше 320kbps)
- Попробуй другой browser (Chrome работает лучше всего)

### Проблема: "HTTPS mixed content"
**Решение:**
- Используй HTTPS для stream URL
- Или используй Cloudflare для прокси

---

## ✅ Готово!

После настройки `VITE_STREAM_URL`:
1. ✅ Radio Player будет использовать твой stream
2. ✅ Auto-DJ будет работать независимо (для Now Playing metadata)
3. ✅ Live stream будет доступен всем слушателям

**Happy broadcasting!** 🎵📻
